<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RAWWR REPTIL - Premium Store</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
      background-color: #f8f9fa;
    }
    
    .navbar {
      backdrop-filter: blur(10px);
      background-color: rgba(33, 37, 41, 0.95) !important;
    }

    .hero-section {
      background: linear-gradient(135deg, #ffffff 0%, #e9ecef 100%);
      padding: 80px 0;
    }

    .product-card {
      border-radius: 20px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      background: white;
      border: 1px solid rgba(0,0,0,0.05) !important;
    }
    
    .product-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.08) !important;
    }

    .card-img-top {
      border-radius: 20px 20px 0 0;
      transition: transform 0.5s ease;
    }

    .product-card:hover .card-img-top {
      transform: scale(1.05);
    }

    .img-wrapper {
      overflow: hidden;
      border-radius: 20px 20px 0 0;
    }

    .price-text {
      color: #198754;
      font-weight: 800;
      letter-spacing: -0.5px;
    }

    .btn-custom {
      border-radius: 12px;
      padding: 10px 20px;
      font-weight: 600;
      transition: all 0.3s;
    }

    .badge-stock {
      background: rgba(25, 135, 84, 0.1);
      color: #198754;
      padding: 4px 12px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 0.75rem;
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top">
  <div class="container">
    <a class="navbar-brand fw-extrabold fs-3" href="/">
      <span class="text-success">RAWWR</span> REPTIL <a href="/">Home</a>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav ms-auto align-items-center">
        <li class="me-lg-4 my-2 my-lg-0">
          <form class="d-flex" role="search" action="/products" method="GET">
            <input 
              class="form-control bg-dark text-white border-secondary me-2 rounded-3" 
              type="search" 
              name="search" 
              placeholder="Cari reptil..."
              aria-label="Search"
            />
            <button class="btn btn-success rounded-3" type="submit">Cari</button>
          </form>
        </li>

          <li class="nav-item">
            <a href="/" class="btn btn-danger btn-custom">Logout</a>
          </li>
       
      </ul>
    </div>
  </div>
</nav>

<section class="hero-section text-center border-bottom">
  <div class="container">
    <h1 class="fw-bolder text-dark display-4 mb-3">LET'S BUY <span class="text-success">RAAWWRTILL</span></h1>
    <p class="text-muted lead mb-4">Temukan reptil impianmu dengan kualitas terbaik dan aman</p>

    <% if (session.role === 'admin') { %>
      <a href="/products/add" class="btn btn-success btn-lg btn-custom shadow-sm px-5">
        ➕ Tambah Produk Baru
      </a>
    <% } %>
  </div>
</section>

<div class="container my-5">
  <div class="row g-4"> 
    
    <% if (dataProduct.length === 0) { %>
      <div class="col-12 text-center py-5">
        <img src="https://cdn-icons-png.flaticon.com/512/6134/6134065.png" alt="Not Found" style="width: 100px; opacity: 0.5;">
        <h3 class="text-muted mt-3">Oops! Reptil tidak ditemukan.</h3>
        <p>Coba kata kunci lain atau kembali ke beranda.</p>
        <a href="/products" class="btn btn-success btn-custom">Lihat Semua Produk</a>
      </div>
    <% } %>

   <% dataProduct.forEach(el => { %>
  <div class="col-md-4 col-sm-6">
    <div class="card product-card h-100 shadow-sm">

      <div class="img-wrapper">
        <img
          src="<%= el.imageURL || 'https://via.placeholder.com/400x300' %>"
          class="card-img-top"
          alt="<%= el.title %>"
          style="height: 220px; object-fit: cover;"
        >
      </div>

      <div class="card-body d-flex flex-column">

        <h5 class="fw-bold mb-1"><%= el.title %></h5>
        <small class="text-muted mb-2">Store: <%= el.storeName %></small>

        <span class="badge-stock mb-2">
          Stock: <%= el.stock %>
        </span>

        <div class="mt-2 mb-3">
          <span class="price-text fs-4">
            <%= formatMoney(el.price) %>
          </span>
        </div>

        <!-- BUTTON ZONE -->
        <div class="mt-auto d-grid gap-2">

          <!-- BUY BUTTON -->
          <% if (session.role === 'buyer') { %>
            <a href="/products/<%= el.id %>/buy" class="btn btn-success btn-custom">
              🛒 Beli Sekarang
            </a>
          <% } %>

          <!-- ADMIN EDIT -->
          <% if (session.role === 'admin') { %>
            <a href="/products/<%= el.id %>/edit" class="btn btn-outline-success btn-custom">
              ✏️ Edit Product
            </a>
          <% } %>

          <!-- ADMIN DELETE -->
          <% if (session.role === 'admin') { %>
            <form action="/products/<%= el.id %>/delete" method="POST">
              <button class="btn btn-danger btn-custom w-100">
                🗑 Delete
              </button>
            </form>
          <% } %>

        </div>

      </div>
    </div>
  </div>
<% }); %>


  </div>
</div>

<footer class="bg-dark text-white py-5 text-center mt-5">
  <div class="container">
    <h4 class="fw-bold mb-3"><span class="text-success">RAWWR</span> REPTIL</h4>
    <p class="text-white-50 mb-4 small">Menghubungkan pecinta hewan eksotis di seluruh Indonesia.</p>
    <div class="mb-4">
      <hr class="bg-secondary opacity-25 w-25 mx-auto">
    </div>
    <small class="text-white-50">&copy; 2026 RAWWR REPTIL. All rights reserved.</small>
  </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>




</body>
</html>